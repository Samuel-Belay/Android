{
  "name": "Android VM DevContainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "mounts": [
    "source=${localWorkspaceFolder}/.android,target=/workspace/.android,type=bind"
  ],
  "forwardPorts": [5901],
  "remoteUser": "vscode"
}
